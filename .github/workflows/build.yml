name: Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 build event."
  validate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 validate event."