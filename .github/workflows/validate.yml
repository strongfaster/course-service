name: Validate
on:
  workflow_run:
    workflows: [Build]
    types: [completed]
    branches: [test]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 ${{ github.event_name }} event."